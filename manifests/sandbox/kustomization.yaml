apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sandbox

resources:
  - app.yaml

patches:
  - path: sandbox.patch.yaml
    target:
      kind: Deployment
      labelSelector: app=sandbox
